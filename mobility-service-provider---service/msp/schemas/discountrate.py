discount_rate_patch = {
    "title": "Patch discount rate",
    "type": "object",
    "properties": {
        "discount_rate": {
            "type": "integer"
        }
    },
    "required": [
        "discount_rate"

    ]
}